import path from 'path'
const rootPath = path.join(__dirname, '../..')
export default {
  title: "Demon Ray's Blog",
  rootPath,
  publicPath: '/public',
  staticPath: '/public/static',
  port: 3000
}
